// check array empty
const arr=[];
if(arr.length===0){ 
    console.log("Array is empty");
}
// check object empty
const user={};
if(Object.keys(user).length===0){
    console.log("Object is empty");
}